"use client";
import { AppProgressBar as ProgressBar } from "next-nprogress-bar";

const ProcessBar = () => {
  return (
    <div>
      <ProgressBar />
    </div>
  );
};

export default ProcessBar;
